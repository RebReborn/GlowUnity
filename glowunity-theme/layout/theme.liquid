<!DOCTYPE html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  
  <!-- GlowUnity Assets -->
  {{ 'glowunity.css' | asset_url | stylesheet_tag }}
  <script src="{{ 'glowunity.js' | asset_url }}" defer></script>
  
  {{ content_for_header }}
</head>

<body>
  {% section 'header' %}
  
  <main id="MainContent">
    {{ content_for_layout }}
  </main>
  
  {% section 'footer' %}
</body>
</html>
